services:
  - type: web
    name: zep-proxy-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        fromService: true
    healthCheckPath: /health
    autoDeploy: true 